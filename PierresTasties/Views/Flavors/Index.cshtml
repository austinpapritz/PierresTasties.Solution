@{
    ViewData["Title"] = "Flavor Page";
}

<h1>@ViewData["Title"]</h1>

@if (@Model.Count == 0)
{
	<h3>There's nothing here</h3>
}

<ul>
    @foreach (Flavor flavor in Model)
    {
			<li>
                @flavor.Name
                <a href="/flavors/details/@flavor.FlavorId" title="Details">🔎</a>
                
                @if (ViewBag.isAdmin)
                {
                    <a href="/flavors/edit/@flavor.FlavorId" title="Edit">✏️</a>
                    <a 
                        href="#" 
                        data-id="@flavor.FlavorId" 
                        data-url="/flavors/delete/@flavor.FlavorId" 
                        class="delete-link"
                        title="Delete"
                    >❌</a>
                }
            </li>
    }
</ul>