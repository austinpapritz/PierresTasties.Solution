@{
    ViewData["Title"] = "Flavor Page";
}

<h1>@ViewData["Title"]</h1>

@if (@Model.Count == 0)
{
	<h3>There's nothing here</h3>
}

<ul>
    @foreach (Flavor exampleModel in Model)
    {
			<li>@exampleModel.Name</li>
    }
</ul>